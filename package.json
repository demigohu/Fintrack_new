{
  "name": "fintrack",
  "version": "1.0.0",
  "scripts": {
    "build": "cd fintrack_frontend && npm run build",
    "dev": "cd fintrack_frontend && npm run dev",
    "start": "cd fintrack_frontend && npm run start",
    "lint": "cd fintrack_frontend && npm run lint"
  },
  "dependencies": {
    "@dfinity/agent": "^3.2.1",
    "@dfinity/candid": "^3.2.1",
    "@dfinity/principal": "^3.2.1",
    "@uniswap/sdk-core": "^7.7.2",
    "@uniswap/v3-sdk": "^3.25.2"
  }
}
